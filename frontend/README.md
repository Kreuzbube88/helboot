# HELBOOT Frontend

React + TypeScript single-page application (ADR-0003). It communicates
with the backend exclusively through the REST API (ADR-0010) and is
fully internationalized — see `src/i18n/locales/`.

See [docs/DEVELOPMENT.md](../docs/DEVELOPMENT.md) for setup, commands
and the i18n rules.
